Explore repository {repository} for the following approved issue brief.

Issue brief:
{issue_brief}

Use the read-only tools to gather repository evidence, then return the required
structured repository map.
