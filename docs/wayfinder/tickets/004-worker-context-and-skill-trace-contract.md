# Worker Context and Skill Trace Contract

## Question

What exact files, events, and results must every Codex or Claude worker receive
and emit? Define `sub_agents.md`, `context.agent`, the task queue location,
portable Skill snapshot references, trace phase schema, evidence retention,
and the acceptance gate for `implement → tdd → code-review`.

## Type

`wayfinder:grilling` (human decision)

## Status

Resolved for MVP
